import { DurationUnit } from "../types";
export declare namespace AnimatedReplace {
    function Horizontal({ index, duration, children }: {
        index: number;
        duration: DurationUnit;
        children: JSX.Element[];
    }): import("react/jsx-runtime").JSX.Element;
}
